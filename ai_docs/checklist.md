# Челист проекта: Мини-дашборд заказов

Используйте этот чек-лист для отслеживания прогресса по каждому этапу. Для каждого нового этапа мы будем создавать отдельный чат.

## Этап 1: Подготовка инфраструктуры
- [x] Зарегистрировать демо-аккаунт в **RetailCRM**.
- [x] Получить API-ключ RetailCRM и базовый URL.
- [x] Создать бесплатный проект в **Supabase**.
- [x] Получить `Project URL` и `Service Role Key` (или `Anon Key`) от Supabase.
- [x] Создать SQL-таблицу `orders` в Supabase.
- [x] Создать бота в **Telegram** через `@BotFather` и получить токен.
- [x] Узнать свой Telegram `CHAT_ID`.
- [x] Зарегистрироваться/авторизоваться в **Vercel** и **GitHub**.
- [x] Создать пустой GitHub репозиторий для проекта.

## Этап 2: Импорт в RetailCRM
- [x] Инициализировать Node.js проект с TypeScript (`npm init`, `tsc --init`).
- [x] Установить зависимости (`axios`, `dotenv`, `typescript`, `ts-node`).
- [x] Создать файл `.env` и добавить ключи от RetailCRM.
- [x] Написать TypeScript-скрипт чтения `mock_orders.json`.
- [x] Реализовать маппинг полей JSON в формат API RetailCRM.
- [x] Реализовать отправку POST-запросов к `/api/v5/orders/create`.
- [x] Запустить скрипт и убедиться, что 50 заказов появились в интерфейсе RetailCRM.

## Этап 3: Синхронизация RetailCRM → Supabase
- [x] Установить библиотеку `@supabase/supabase-js`.
- [x] Добавить ключи Supabase в `.env`.
- [x] Написать скрипт для получения списка заказов из RetailCRM (`/api/v5/orders`).
- [x] Написать логику обработки данных (расчет итоговой суммы заказа, если нужно).
- [x] Реализовать запись/обновление (upsert) полученных заказов в таблицу `orders` в Supabase.
- [x] Запустить скрипт и проверить, что данные появились в таблице Supabase.

## Этап 4: Дашборд на Vercel
- [x] Создать проект Next.js с TypeScript в корне репозитория.
- [x] Настроить подключение к Supabase в Next.js (утилита `supabase-client`).
- [x] Создать функцию для получения заказов из БД.
- [x] Установить библиотеку графиков (например, `recharts`).
- [x] Сверстать страницу дашборда с графиком (например, сумма заказов или кол-во заказов).
- [x] Закоммитить и запушить код в GitHub.
- [x] Настроить деплой в Vercel (привязать репозиторий).
- [x] Прописать переменные окружения Supabase в настройках Vercel.
- [x] Проверить работу дашборда по публичной ссылке Vercel.

## Этап 5: Telegram-уведомления
- [x] Создать API Route в Next.js (например, `/api/webhooks/retailcrm`).
- [x] Написать логику приема POST-запроса от RetailCRM.
- [x] Написать логику проверки суммы заказа (> 50,000 ₸).
- [x] Написать функцию отправки сообщения через Telegram Bot API.
- [x] Задеплоить обновленный код на Vercel.
- [x] Настроить триггер/вебхук в панели управления RetailCRM на URL нашего API Route.
- [x] Сделать тестовый заказ на сумму > 50,000 ₸ в RetailCRM.
- [x] Проверить получение уведомления в Telegram.

## Финал
- [x] Оформить `README.md` в репозитории (описание, стек, как запустить, скриншоты).
- [x] Добавить в `README.md` описание промптов и процесса работы с AI, как требовалось в задании.
- [x] Собрать все ссылки (Дашборд, GitHub) и скриншот Telegram-бота для отправки.
